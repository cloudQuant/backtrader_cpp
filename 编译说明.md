# Backtrader C++ 编译说明

## 为什么 `bash run_tests.sh` 能编译成功，但直接在tests目录编译会失败？

### 关键原因分析

#### 1. 依赖关系
tests目录的编译**依赖于主项目的核心库** `libbacktrader_core.a`。

tests/CMakeLists.txt 中配置如下：
```cmake
# 查找父目录的库
find_library(BACKTRADER_CORE_LIB backtrader_core PATHS ${CMAKE_SOURCE_DIR}/.. NO_DEFAULT_PATH)

# 如果找不到库文件，直接使用路径
if(NOT BACKTRADER_CORE_LIB)
    set(BACKTRADER_CORE_LIB ${CMAKE_SOURCE_DIR}/../libbacktrader_core.a)
```

这意味着tests需要 `../libbacktrader_core.a` 文件存在。

#### 2. run_tests.sh 的成功流程

```bash
# 1. 先编译主项目（不包含tests）
cd /path/to/backtrader_cpp
cmake . -DCMAKE_BUILD_TYPE=Debug -DBUILD_TESTS=OFF
cmake --build . --config Debug --parallel 8
# → 生成 libbacktrader_core.a

# 2. 然后编译tests
cd tests/build
cmake .. -DCMAKE_BUILD_TYPE=Debug
cmake --build . --config Debug --parallel 8
# → tests可以找到 ../libbacktrader_core.a，编译成功
```

#### 3. 直接在tests目录编译失败的原因

如果核心库不存在：
```bash
cd tests
cmake -B build       # ❌ 找不到 ../libbacktrader_core.a，配置失败
cmake --build build  # ❌ 无法编译
```

### 正确的编译顺序

#### 方法1：使用 run_tests.sh（推荐）
```bash
cd /path/to/backtrader_cpp
bash run_tests.sh
```

#### 方法2：手动分步编译
```bash
# 步骤1：编译核心库
cd /path/to/backtrader_cpp
cmake . -DCMAKE_BUILD_TYPE=Debug -DBUILD_TESTS=OFF
cmake --build . --parallel 8

# 步骤2：编译tests
cd tests
cmake -B build -DCMAKE_BUILD_TYPE=Debug
cmake --build build --parallel 8

# 步骤3：运行tests（需要设置库路径）
cd build
export LD_LIBRARY_PATH=/usr/lib/x86_64-linux-gnu:$LD_LIBRARY_PATH
./test_ind_sma
```

### 当前状态验证

✅ 核心库已编译：
```bash
$ ls -lh libbacktrader_core.a
-rw-rw-r-- 1 yun yun 72M Oct  9 21:11 libbacktrader_core.a
```

✅ 测试已编译：
```bash
$ ls tests/build/test_* | wc -l
83
```

✅ 测试可运行：
```bash
$ LD_LIBRARY_PATH=/usr/lib/x86_64-linux-gnu:$LD_LIBRARY_PATH ./test_ind_sma
[==========] Running 11 tests from 2 test suites.
...
[  PASSED  ] 11 tests.
```

### 总结

| 方法 | 是否成功 | 原因 |
|------|---------|------|
| `bash run_tests.sh` | ✅ | 先编译核心库，再编译tests |
| 直接 `cd tests && cmake -B build` | ❌ | 核心库不存在时失败 |
| 先编译主项目，再编译tests | ✅ | 正确的依赖顺序 |

**关键点**：tests目录不是独立的项目，它依赖主项目的核心库，必须先编译主项目。
